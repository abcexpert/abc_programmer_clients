Конечно! Давайте создадим пример простой Python-программы, которая будет выполнять полезную задачу. Предположим, мы хотим создать программу, которая будет запрашивать у пользователя имя, возраст и город проживания, а затем выводить приветственное сообщение с этой информацией.

Вот как это можно сделать:

```python
def main():
    # Запрашиваем у пользователя информацию
    name = input("Введите ваше имя: ")
    age = input("Введите ваш возраст: ")
    city = input("Введите ваш город проживания: ")

    # Форматируем приветственное сообщение
    message = f"Привет, {name}! Вам {age} лет, и вы живёте в городе {city}."
    
    # Выводим сообщение на экран
    print(message)

# Запускаем основную функцию
if __name__ == "__main__":
    main()
```

Давайте разберем эту программу подробнее:

1. Мы определяем функцию `main()`, которая содержит основную логику программы.
2. В функции `main()` мы запрашиваем у пользователя его имя, возраст и город проживания с помощью функции `input()`.
3. Мы используем f-строку для форматирования приветственного сообщения.
4. Выводим сформированное сообщение на экран с помощью функции `print()`.
5. Проверка `if __name__ == "__main__":` гарантирует, что основная функция будет выполнена только если этот скрипт запущен напрямую, а не импортирован как модуль в другой скрипт.

Вы можете скопировать этот код в файл с расширением `.py` (например, `greeting.py`) и запустить его в Python, чтобы увидеть, как он работает.
# debugged